import { Injectable } from '@nestjs/common';

@Injectable()
export class GroupService {

    async joinRoom(userName:string, room:string) {
        // check if user in room

    }

    async leaveRoom(userName:string, room:string) {
        // check if user in room

    }

    async getRoomUsers(userName:string, room:string) {
        // check if user in room

    }

}